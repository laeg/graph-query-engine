#****************************************************
# Title: 	EDF ECM Migration Export - Build Properties 						
# Author: 	LAEG																
#****************************************************

#****************************************************
# Directory Properties									
#****************************************************
java.dir=${svn.dir}/java
project.dir=${java.dir}/MigrationAuditTransmittal

#****************************************************
# Library Directory Properties									
#****************************************************
lib.dir=${project.dir}/lib
documentum.lib.dir=${project.dir}/lib/documentum
edf.lib.dir=${project.dir}/lib/edf
jdbc.lib.dir=${project.dir}/lib/jdbc
junit.lib.dir=${project.dir}/lib/junit
log4j.lib.dir=${project.dir}/lib/log4j

#****************************************************
# Build Directory Properties									
#****************************************************
build.dir=${project.dir}/build
build.src.dir=${project.dir}/src
build.class.dir=${project.dir}/class
build.package.dir=${edf.lib.dir}

#****************************************************
# Configuration Directory Properties									
#****************************************************
config.dir=${project.dir}/config
log4j.config.dir=${config.dir}/log4j

#****************************************************
# Configuration Directory Properties - Log4J									
#****************************************************
log4j.config.dir=${config.dir}/log4j
gen.log4j.config.dir=${log4j.config.dir}/gen
dev.log4j.config.dir=${log4j.config.dir}/dev
mig.log4j.config.dir=${log4j.config.dir}/mig
prod.log4j.config.dir=${log4j.config.dir}/prod

#****************************************************
# Configuration Directory Properties - Documentum								
#****************************************************
documentum.config.dir=${config.dir}/documentum
dev.documentum.config.dir=${documentum.config.dir}/dev
mig.documentum.config.dir=${documentum.config.dir}/mig
prod.documentum.config.dir=${documentum.config.dir}/prod

#****************************************************
# Package Properties									
#****************************************************
migration.jar=edf-ecm-audit-transmittal-migration-export

#****************************************************
# Class Properties									
#****************************************************
export.transmittal.class=com.edf.nnb.ba.ecm.migration.StartTransmittalMigration

#****************************************************
# Repository Arguments						
#****************************************************
mig.repository=ECM_MIG
mig.username=dm_migration_user_1
mig.password=M0r3f00d
mig.startYear=2009
mig.startMonth=08
mig.endYear=2014
mig.endMonth=07
mig.environment=MIG